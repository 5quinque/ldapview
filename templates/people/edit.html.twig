{% extends 'base.html.twig' %}

{% block title %}Edit People{% endblock %}

{% block body %}
    <!-- Begin Page Content -->
    <div class="container-fluid">
        <!-- Page Heading -->
        <div class="d-sm-flex align-items-center justify-content-between mb-4 mt-4">
            <h1 class="h3 mb-0 text-gray-800">Edit People</h1>
            <a href="{{ path('people_show', {'uid': person.uid}) }}"
               class="d-none d-sm-inline-block btn btn-sm btn-primary btn-icon-split shadow-sm"><span
                        class="icon text-white-50"><i
                            class="fas fa-arrow-alt-circle-left"></i></span><span class="text">Cancel</span></a>
        </div>
        {{ include('people/_form.html.twig', {'form_title': 'Edit Person', 'button_label': 'Update'}) }}
        {{ include('people/_delete_form.html.twig') }}
    </div>
{% endblock %}
